# ECHO//SHIFT 개발 로드맵

이 문서는 AI가 임의로 새 게임 기능을 발명하는 곳이 아니라, **사용자가 승인한 개발 목표를 기록하는 단일 소스(source of truth)** 이다.
OpenRouter Supervisor(v0.6 이상)는 이 문서를 읽기 전용으로 참고해 `NEXT_TASK.md`의 근거로 삼을 수 있지만, 이 문서 자체를 수정하지 않는다. 완료 체크(`[x]`)는 사람 또는 이후 버전의 worker 단계에서만 한다.

## 상태 표기

- `[ ]` TODO — 아직 시작하지 않음
- `[~]` IN_PROGRESS — 진행 중
- `[x]` DONE — 완료 (완료 조건을 충족하고 사람이 확인함)
- `[!]` BLOCKED — 진행하려면 사람의 설계 결정이 먼저 필요함

각 항목은 가능하면 **목표 / 근거·의도 / 완료 조건** 세 가지를 포함한다. 여기 적힌 근거는 저장소에 이미 존재하는 리포트/로그/소스 파일에서만 가져왔다 — 구체적인 신규 적·스킬·수치·기능은 이 문서에서 임의로 만들어내지 않았고, 방향과 목표만 기록한다.

---

## 1. [ ] 5~7분 안정적인 런 달성

- **목표**: 한 판의 평균 총 런 시간이 5~7분 사이에서 안정적으로 나오게 하고, 보스 도달률·클리어율의 편차를 줄인다.
- **근거/의도**: `RUN_REPORT.md` — 평균 총 런 BASIC 5:01 / SMART 4:52, 평균 클리어 시간 BASIC 5:44 / SMART 5:17로 이미 5~7분 범위 안에 들어와 있지만, 보스 도달률은 BASIC 62% / SMART 69%, 클리어율은 BASIC 60% / SMART 66%에 그쳐 "안정적"이라 보기는 이르다. `LONG_BUILD_REPORT.md`의 빌드별 보스 도달률(68%~79%)도 빌드에 따라 편차가 크다는 같은 신호를 보여준다.
- **완료 조건**: `tests/sim/sim_run.js`(→ `RUN_REPORT.md`) 또는 `tests/sim/long_build.js`(→ `LONG_BUILD_REPORT.md`)를 다시 실행했을 때, 평균 총 런 시간이 5~7분 범위를 벗어나지 않으면서 보스 도달률·클리어율의 봇/빌드 간 편차가 지금보다 줄어든다. 구체적인 목표 수치(몇 %까지 줄일지)는 이 항목에서 확정하지 않으며 별도 승인이 필요하다.

## 2. [ ] 200~220초 구간에 몰리는 사망 원인 완화

- **목표**: 일반 구간 후반(약 200~280초)에 사망이 몰리는 현상과, 그 구간에서 특정 적 하나가 사망 원인을 지배하는 현상을 완화한다.
- **근거/의도**: `DEATH_REPORT.md`의 20초 버킷 사망 분포 — BASIC/SMART 모두 200~220초·220~240초·260~280초 구간에 사망이 몰려 있고(BASIC 260~280초 26건으로 최다, SMART도 260~280초 26건), 두 봇 모두 해당 구간을 "난도 스파이크 의심"으로 명시하고 있다. 같은 리포트의 **DEATH SOURCE DOMINANCE** 판정에 따르면 `warden`이 마지막 사망 원인의 BASIC 78% / SMART 62%를 차지해 기준(60%)을 넘는다. 스폰 밴드 자체는 `src/js/00_config.js`의 `RUN.phases`에 정의되어 있다.
- **완료 조건**: `DEATH_REPORT.md`를 다시 생성했을 때 해당 구간의 사망 비중이 줄고, `warden`의 DEATH SOURCE DOMINANCE 초과 상태가 해소되거나 완화된다. 어떤 수치·메커니즘으로 완화할지는 이 항목에서 정하지 않는다.

## 3. [ ] 난이도 곡선 보정

- **목표**: 일반 구간 전체(PHASE 1~3)에 걸친 체감 난이도 상승이 급격하지 않고 고르게 이어지도록 조정한다.
- **근거/의도**: `DEATH_REPORT.md`의 PHASE별 사망 비중 — BASIC(PHASE1 0% · PHASE2 25% · PHASE3 75%), SMART(PHASE1 0% · PHASE2 20% · PHASE3 77%) — 로 PHASE 3(`src/js/00_config.js`의 `RUN.phases[2]`, COLLAPSE 구간)에 사망이 집중되어 있다. 이는 1번·2번 항목과 같은 데이터(DEATH_REPORT.md)에서 나온 다른 측면이며, PHASE 단위로 난이도가 어떻게 분배되는지를 본다는 점에서 구분되는 항목이다.
- **완료 조건**: `DEATH_REPORT.md`의 PHASE별 사망 비중이 지금보다 고르게 분산된다. 목표 비율은 이 항목에서 확정하지 않는다.

## 4. [ ] ECHO 시스템 차별화 강화

- **목표**: ECHO를 적극적으로 활용하는 플레이와 그렇지 않은 플레이 사이의 결과 차이를 더 뚜렷하게 만든다.
- **근거/의도**: `TEST_REPORT.md`의 BOT A/B/C 비교(A는 ECHO API 미사용) — ECHO 피해비율 A 27% / B 63% / C 67%, SYNC 대기 A 4.58s vs B/C 1.40~1.44s로 차이는 있지만, `RUN_REPORT.md`의 BASIC/SMART(둘 다 ECHO 사용) ECHO 처치 비율은 48%/49%로 거의 같다. `tests/sim/ablation.js`(→ `ABLATION_REPORT.md`)가 이미 SMART의 WARDEN 플랭크/PHASE 유도/ELITE SYNC 판단을 하나씩 제거해 기여도를 측정하는 구조를 갖추고 있어, 이 항목의 진행 여부를 그 결과로 가늠할 수 있다.
- **완료 조건**: `tests/sim/ablation.js`/`tests/sim/upgrade_benchmark.js` 등 기존 벤치마크로 측정했을 때 ECHO 활용 유무·숙련도에 따른 성과 차이가 지금보다 뚜렷해진다. 구체적인 목표 수치나 새 메커니즘은 이 항목에서 정하지 않는다.

## 5. [ ] 엘리트 다양화

- **목표**: 현재 2종(SYNC LOCK, RELAY CORE)뿐인 ELITE 특성의 다양성을 늘린다.
- **근거/의도**: README.md("v2.5 추가 사항 (ELITE PRESSURE)")와 `src/js/57_elite.js`에 정의된 ELITE 특성은 SYNC LOCK / RELAY CORE 두 가지뿐이다. `ELITE_REPORT.md`의 성공 기준 판정을 보면 기존 2종도 아직 완성 상태는 아니다 — SYNC LOCK은 SMART 처치 시간 감소가 14%로 목표(약 20% 이상)에 미달했고, RELAY CORE도 "SMART 평균 처치 시간이 IGNORE보다 느리지 않아야 함" 기준에서 미달(11.08s vs 10.31s)이다.
- **완료 조건**: 새 ELITE 특성이 추가되거나, 최소한 기존 2종이 `ELITE_REPORT.md`의 미달 기준을 충족한 뒤 다양화를 논의할 수 있는 상태가 된다. 새 특성의 구체적인 행동·수치·트리거 조건은 이 로드맵 항목에서 정하지 않으며, 별도의 설계 승인이 필요하다.

## 6. [ ] 보스 다양화

- **목표**: 현재 1종(PARADOX CORE)뿐인 보스의 다양성을 늘린다.
- **근거/의도**: README.md — `src/js/65_boss.js` = PARADOX CORE가 유일한 보스로 명시되어 있고, `tests/unit/unit_boss.js`·`tests/sim/sim_boss.js` 모두 이 단일 보스만을 전제로 작성되어 있다.
- **완료 조건**: 새 보스가 추가되거나, 최소한 기존 단일 보스 체계에서 다양화를 시작할 수 있는 확장 지점(패턴 구성, 난이도 스케일링 등)이 설계 문서 수준에서 식별된다. 신규 보스의 구체적인 디자인(패턴, 수치, 이름 등)은 이 로드맵 항목에서 정하지 않으며, 별도의 설계 승인이 필요하다.
